

  <title>Error <%= err.statusCode || 500 %></title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f9f9f9;
      color: #333;
      padding: 40px;
    }

    .container {
      background: white;
      padding: 30px;
      border-radius: 8px;
      max-width: 800px;
      margin: 0 auto;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }

    h1 {
      font-size: 2.5rem;
      color: #d33;
      margin-bottom: 10px;
    }

    h2 {
      font-size: 1.5rem;
      margin-bottom: 20px;
    }

    pre {
      background: #f4f4f4;
      padding: 10px;
      border-left: 4px solid #ccc;
      overflow-x: auto;
    }

    a {
      display: inline-block;
      margin-top: 20px;
      color: #0066cc;
      text-decoration: none;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Error <%= err.statusCode || 500 %></h1>
    <h2><%= err.message || "Something went wrong." %></h2>

    <% if (err.stack) { %>
      <pre><%= err.stack %></pre>
    <% } %>

    <a href="/">← Back to Home</a>
  </div>
</body>
