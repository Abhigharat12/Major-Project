<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Error <%= err.statusCode || 500 %></title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', sans-serif;
      background: url('https://images.unsplash.com/photo-1508704019882-f9cf40e475b4?auto=format&fit=crop&w=1470&q=80') no-repeat center center fixed;
      background-size: cover;
      color: white;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
    }

    .container {
      background: rgba(0, 0, 0, 0.75);
      padding: 40px;
      border-radius: 15px;
      max-width: 600px;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.4);
      text-align: center;
      animation: fadeIn 0.5s ease;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: scale(0.95); }
      to { opacity: 1; transform: scale(1); }
    }

    h1 {
      font-size: 3rem;
      color: #ff4d4d;
      margin-bottom: 15px;
    }

    p {
      font-size: 1.3rem;
      margin-bottom: 20px;
    }

    .alert-danger {
      background: rgba(255, 77, 77, 0.1);
      color: #ffb3b3;
      padding: 15px;
      border-left: 5px solid #ff4d4d;
      border-radius: 8px;
      margin-bottom: 20px;
      text-align: left;
    }

    .alert-danger ul {
      margin: 0;
      padding-left: 20px;
    }

    a {
      display: inline-block;
      color: #ffffff;
      text-decoration: underline;
      margin-top: 15px;
      font-weight: 600;
      transition: color 0.3s ease;
    }

    a:hover {
      color: #ffb3b3;
    }
  </style>
</head>
<body>
  <main class="container">
    <h1>Error <%= err.statusCode || 500 %></h1>
    <p><%= err.message %></p>

    <% if (err.statusCode === 400) { %>
      <div class="alert-danger">
        <ul>
          <% err.message.split(",").forEach(msg => { %>
            <li><%= msg.trim() %></li>
          <% }) %>
        </ul>
      </div>
    <% } %>

    <a href="javascript:history.back()">⬅ Go Back</a>
  </main>
</body>
</html>
