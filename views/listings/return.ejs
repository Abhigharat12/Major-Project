<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Payment Successful</title>
  <style>
    body {
      font-family: sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background-color: #f0f0f0;
    }
    .container {
      background-color: white;
      padding: 2rem;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
      text-align: center;
    }
    h1 {
      color: #4CAF50;
    }
    #payment-details {
      margin-top: 1.5rem;
      text-align: left;
    }
    ul {
      list-style-type: none;
      padding: 0;
    }
    li {
      margin: 0.5rem 0;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Payment <%= status === 'success' ? 'Successful' : 'Failed' %>!</h1>

    <% if (status === 'success') { %>
      <p>Thank you for your payment. Here are the details:</p>
      <div id="payment-details">
        <ul>
          <% if (orderId) { %><li><strong>Order ID:</strong> <%= orderId %></li><% } %>
          <% if (amount) { %><li><strong>Amount:</strong> ₹<%= amount %></li><% } %>
          <% if (listingTitle) { %><li><strong>Listing:</strong> <%= listingTitle %></li><% } %>
        </ul>
      </div>
    <% } else { %>
      <p>Your payment could not be processed.</p>
      <% if (error) { %><p style="color: red;"><%= error %></p><% } %>
    <% } %>

    <a href="/">Go to Homepage</a>
  </div>
</body>
</html>
